def apply_enrichment(cfg, updates):
    """
    Apply enrichment updates back to the prospects sheet.
    Each update should be keyed by website_url.
    Stub only — no-op for now.
    """
    if not updates:
        return 0

    # TODO: map updates to sheet rows and write back
    return len(updates)